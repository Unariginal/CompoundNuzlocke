package me.unariginal.compoundnuzlocke.handlers;

import com.cobblemon.mod.common.api.battles.interpreter.BattleMessage;
import com.cobblemon.mod.common.api.battles.model.PokemonBattle;
import net.fabricmc.fabric.api.event.Event;
import net.fabricmc.fabric.api.event.EventFactory;

public interface BattleHandler {
//    Event<battleEnd> BATTLE_END_EVENT = EventFactory.createArrayBacked(BattleEnd.class, (listeners) -> (battle, message) -> );
//        for (battleEnd listener : listeners) {
//            PokemonBattle
//    }
//    interface battleEnd {
//        void bEnd(PokemonBattle var1, BattleMessage var2);
//    }
}
